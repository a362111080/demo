<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.zero.egg.dao.SystemMapper">
    <select id="checklogin"  resultType="com.zero.egg.model.LoginInfo" parameterType="String">
        SELECT * FROM sys_userstroes_info a left join sys_user_info b on a.strusercode=b.strusercode WHERE a.strpassname=#{strUserName} and  a.strpassword=#{passWord} and b.LngIsBoss=1
    </select>
</mapper>