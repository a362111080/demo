<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.zero.egg.dao.SystemMapper">
    <select id="checklogin"  resultType="java.lang.Integer" parameterType="String">
        SELECT count(1) FROM sys_user_info WHERE strUserName=#{strUserName} and  strPassWord=#{passWord} and LngIsBoss=1
    </select>
</mapper>