<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
        
<mapper namespace="com.zero.egg.dao.StudentMapper">
    <select id="getStudentByID" parameterType="int" resultType="com.zero.egg.model.Student">
         SELECT * FROM student WHERE id=#{id}
    </select>
</mapper>